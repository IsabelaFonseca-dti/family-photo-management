import styled from '@emotion/styled';

export const ErrorContainer = styled.main({
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  height: '100vh',
  fontSize: '1.5rem',
  textAlign: 'center',
});
